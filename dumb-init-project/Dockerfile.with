FROM python:3.9-slim

# Install dumb-init and the 'ps' command
RUN apt-get update && apt-get install -y dumb-init procps && apt-get clean && rm -rf /var/lib/apt/lists/*

WORKDIR /app
COPY app.py .

ENTRYPOINT ["dumb-init", "--"]
CMD ["python", "app.py"]